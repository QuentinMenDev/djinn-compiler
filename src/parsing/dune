(library
 (name parsing)
 (libraries core fmt menhirLib ast)
 (preprocess
  (pps bisect_ppx --conditional))
 (lint
  (pps ppx_js_style -annotated-ignores -styler -pretty -dated-deprecation)))

(menhir
 (modules parser))

(ocamllex lexer)
